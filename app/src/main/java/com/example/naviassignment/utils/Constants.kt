package com.example.naviassignment.utils

class Constants {
    companion object {
        const val SERVER_DATE_TIME_FORMAT = "yyyy-MM-dd'T'HH:mm:SSS"
        const val UI_DATE_FORMAT = "dd MMM yyyy"
        const val UI_TIME_FORMAT = "HH:mm:SS"
        const val COMMIT_TYPE_CLOSED = "close"
    }
}